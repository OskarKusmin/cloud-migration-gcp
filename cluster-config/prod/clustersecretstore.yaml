apiVersion: external-secrets.io/v1alpha1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-manager
spec:
  provider:
    gcpsm:
      projectID: voyager-prod-489709
      auth:
        workloadIdentity:
          clusterLocation: europe-north1
          clusterName: voyager-prod
          clusterProjectID: voyager-prod-489709
          serviceAccountRef:
            name: prod-external-secrets
            namespace: external-secrets