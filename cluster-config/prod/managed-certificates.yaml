apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: public-cert
  namespace: sample-app
spec:
  domains:
    - frontend.prod-public.<DOMAIN>
    - backend.prod-public.<DOMAIN>
    - www.<DOMAIN>