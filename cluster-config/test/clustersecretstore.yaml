apiVersion: external-secrets.io/v1alpha1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-manager
spec:
  provider:
    gcpsm:
      projectID: voyager-test-500708
      auth:
        workloadIdentity:
          clusterLocation: europe-north1-b
          clusterName: voyager-test
          clusterProjectID: voyager-test-500708
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets