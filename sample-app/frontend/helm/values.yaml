replicaCount: 1

image:
  repository: <GCP_REGION>-docker.pkg.dev//voyager/frontend
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 3000

nodeSelector:
  role: main

tolerations: []

backendUrl: "http://voyager-backend:8080"
apiUrl: ""

ingress:
  enabled: false
  className: ""
  host: ""
  additionalHosts: []
  annotations: {}