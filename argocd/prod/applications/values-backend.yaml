backend:
  image:
    repository: europe-north1-docker.pkg.dev/voyager-shared-500708/voyager/backend
  config:
    CLIENT_URL: "https://www.kood-voyager.com"
    SERVER_PORT: ":8080"
    RUN_MIGRATION: "true"
  externalSecrets:
    enabled: true
    storeName: gcp-secret-manager
    dbCredentialsSecret: voyager-prod-db-credentials
    jwtKeySecret: voyager-prod-jwt-key
  ingress:
    enabled: true
    host: backend.prod-public.kood-voyager.com
    annotations:
      networking.gke.io/managed-certificates: public-cert